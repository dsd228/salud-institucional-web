// Ejemplo de configuración multilenguaje (opcional)
import { createContext } from "react";
export const idiomaContexto = createContext("es");
// Para ampliar, integrar i18next o react-intl en toda la app
